---
id: index
title: Overview
sidebar_label: Overview
---

# Platform Overview

The **Atlas Platform** is a collaborative environment for designing and deploying **AI-driven workflows** that generate and transform creative content — from images and textures to fully textured 3D assets.

At its core, Atlas brings together **visual workflow design**, **AI model orchestration**, and **API-based integration** into a single, streamlined system built for both artists and developers.

---

## 🧠 What Atlas Does

Atlas allows users to:
- **Design workflows visually** using a node-based graph editor.
- **Combine open-source AI models** into structured pipelines for image, mesh, and multimodal generation.
- **Experiment and iterate** interactively within a unified studio interface.
- **Export complete workflows as APIs**, enabling automation or integration into DCC tools such as Unreal Engine or Houdini.

This visual-first approach bridges the gap between creative experimentation and technical implementation — turning AI generation into a repeatable, versioned, and shareable process.

---

## 🧩 How It Works — The Core Idea

Every workflow in Atlas is composed of **nodes**, each representing a specific operation such as “Image to 3D,” “Text to Image,” or “Depth Refinement.”
Nodes can be connected to form a data flow, where each step transforms or enhances the output of the previous one.

Each node contains:
- A configurable **backend selector** — choose which model or engine to use.
- Adjustable **parameters** — fine-tune generation behavior.
- Versioned **settings** — ensuring deterministic and reproducible results.

When a workflow is ready, it can be **exported as an API** with a unique identifier and example payloads for immediate use in production.

---

## ⚙️ What Makes Atlas Different

- **Visual-first creation**
  Build complex AI pipelines without code. The graph interface makes experimentation intuitive and fast.

- **Model-agnostic design**
  Each node can use different AI backends — from diffusion models to mesh reconstruction networks — depending on its purpose.

- **Deterministic, reproducible output**
  Every backend and parameter set is versioned, allowing consistent results across sessions or collaborators.

- **Seamless integration**
  Export workflows as APIs that can be executed from Unreal Engine, command-line tools, or other applications.

- **Unified workspace**
  All projects, assets, and workflows live in one environment, supporting both experimentation and production deployment.

---

## 🚀 Typical Use Cases

- **Artists and Technical Artists**
  Rapidly prototype and iterate on procedural or AI-assisted assets without touching code.

- **Developers and Integrators**
  Turn visual workflows into callable APIs for automation, pipelines, or tool integration.

- **Studios and Teams**
  Standardize creative processes, ensuring deterministic output and version-tracked workflows across departments.

---

## 🔗 Next Steps

To understand how Atlas is structured and how to begin creating your first workflow, continue with:
➡️ [Projects Overview](./projects_overview.md)