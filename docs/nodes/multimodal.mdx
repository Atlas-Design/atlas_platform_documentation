---
id: multimodal
title: Multimodal
sidebar_label: Multimodal
---

# Multimodal & Precise Text Edit Nodes

<div style={{display:'flex', justifyContent:'center', marginBottom:'20px'}}>
  <div style={{width:'100%'}}>
    ![](/img/node_introduction/multimodal/nodeintro-multimodal.png)
  </div>
</div>

Atlas provides two powerful image-editing nodes that **both require text + at least one image input**:

- **Multimodal Node**
- **Precise Text Edit Node**

They share the same core idea:  
You provide an image (or multiple images) and a text instruction, and the node generates a modified image using the chosen backend.

They differ in how much freedom the model has.  
Multimodal allows **fully generative edits**, while Precise Text Edit keeps the **original image untouched except for the specified modification**.

---

# 1. Overview

## Multimodal Node
A flexible, image-conditioned generation engine.

- Requires text + image(s)
- Merges all input images with the instruction
- Allows scene transformations, object replacement, isolation
- Produces new images guided by the provided visuals
- Different backends = different image generation models

## Precise Text Edit Node
A constrained version that **preserves the original image**.

- Requires text + one image
- Performs **targeted edits only**
- Maintains composition, style, perspective, lighting
- Perfect for additive changes (e.g., “add vines”, “add lanterns”, “make it snowy”)
- Can change aspect ratio to expand the scene

---

# 2. Inputs

Both nodes share the same input structure:

### Required:
- **Text prompt**
- **At least one image**

### Optional:
- **Multiple images** (Multimodal only)

The nodes combine the visual input(s) with your instruction to generate the new output.

---

# 3. Backends

You may choose from multiple backend generative models.  
Each backend interprets images differently:

- Some are more realistic  
- Some preserve structure better  
- Some are more stylized  
- Some follow prompts more aggressively  

Switching backend changes the artistic and structural behavior.

<div style={{display:'flex', justifyContent:'center', marginBottom:'20px'}}>
  <div style={{width:'50%'}}>
    ![](/img/node_introduction/multimodal/backends-multimodal.png)
  </div>
</div>

---

# 4. Seed Control

Changing the seed gives new variations while keeping:

- the same structure  
- the same instruction  
- the same conditioning images  

Keeping the seed = reproducible outputs.

---

# 5. Example Use Cases — Multimodal Node

## A. Style Transformation of a Scene
Turn a normal bookshelf into a **fantasy enchanted bookshelf**.

<div style={{display:'flex', justifyContent:'center', marginBottom:'20px'}}>
  <div style={{width:'100%'}}>
    ![](/img/node_introduction/multimodal/seed1-multimodal.png)
  </div>
</div>

---

## B. Variation with Seed Change
Same prompt + same image → different seeds → different variations.

<div style={{display:'flex', justifyContent:'center', marginBottom:'20px'}}>
  <div style={{width:'100%'}}>
    ![](/img/node_introduction/multimodal/seed3-multimodal.png)
  </div>
</div>
<div style={{display:'flex', justifyContent:'center', marginBottom:'20px'}}>
  <div style={{width:'100%'}}>
    ![](/img/node_introduction/multimodal/seed2-multimodals.png)
  </div>
</div>

---

## C. Restyling While Preserving Layout
Transform the bookshelf into an **old, damaged one** while keeping the structure.

<div style={{display:'flex', justifyContent:'center', marginBottom:'20px'}}>
  <div style={{width:'100%'}}>
    ![](/img/node_introduction/multimodal/old-multimodal.png)
  </div>
</div>

---

## D. Isolate an Object with Neutral Background
Perfect for Image-to-3D workflows.

<div style={{display:'flex', justifyContent:'center', marginBottom:'20px'}}>
  <div style={{width:'100%'}}>
    ![](/img/node_introduction/multimodal/isolate-multimodal.png)
  </div>
</div>

---

## E. Multi-Image Conditioning for Object Replacement
Replace specific items on a shelf using three reference object images.

<div style={{display:'flex', justifyContent:'center', marginBottom:'20px'}}>
  <div style={{width:'100%'}}>
    ![](/img/node_introduction/multimodal/replace-multimodal.png)
  </div>
</div>

<div style={{display:'flex', justifyContent:'center', marginBottom:'20px'}}>
  <div style={{width:'100%'}}>
    ![](/img/node_introduction/multimodal/output-multimodal.png)
  </div>
</div>

---

# 6. Example Use Cases — Precise Text Edit Node

The **Precise Text Edit Node** keeps the original image intact, modifying only what you request.

---

### A. Adding Details to a Generated Asset  
Example: adding decorative elements or small props without changing the underlying concept.

<div style={{display:'flex', justifyContent:'center', marginBottom:'20px'}}>
  <div style={{width:'100%'}}>
    ![](/img/node_introduction/multimodal/precisetext1-multimodal.png)
  </div>
</div>

---
### B. Removing Objects  

<div style={{display:'flex', justifyContent:'center', marginBottom:'20px'}}>
  <div style={{width:'100%'}}>
    ![](/img/node_introduction/multimodal/precisetext3-multimodal.png)
  </div>
</div>

---

### C. Changing the Design Style
You can restyle the asset into a new design language (e.g., Scandinavian / futuristic / gothic) while keeping the **same layout and camera**.

<div style={{display:'flex', justifyContent:'center', marginBottom:'20px'}}>
  <div style={{width:'100%'}}>
    ![](/img/node_introduction/multimodal/precisetext2-multimodal.png)
  </div>
</div>

Changing the **seed** gives controlled alternatives of the *same style*.

<div style={{display:'flex', justifyContent:'center', marginBottom:'20px'}}>
  <div style={{width:'100%'}}>
    ![](/img/node_introduction/multimodal/precisetext2seed2-multimodal.png)
  </div>
</div>
<div style={{display:'flex', justifyContent:'center', marginBottom:'20px'}}>
  <div style={{width:'100%'}}>
    ![](/img/node_introduction/multimodal/precisetext2seed1-multimodal.png)
  </div>
</div>

---

### D. Changing Image Aspect Ratio  

<div style={{display:'flex', justifyContent:'center', marginBottom:'20px'}}>
  <div style={{width:'100%'}}>
    ![](/img/node_introduction/multimodal/precisetext-aspectratio-multimodal.png)
  </div>
</div>

---

# 7. Capabilities Summary

### Multimodal Node
- generative transformations   
- scene edits  
- object replacement  
- object isolation  
- image mixing  
- multi-image conditioning  

### Precise Text Edit Node
- preserves the original image  
- applies minimal, targeted changes  
- ideal for controlled additions  
- supports aspect-ratio changes  
- keeps lighting/composition intact  

---

# 8. Node Summary

Both nodes require **text + image input** and work with backend models, but their behavior differs:

### **Multimodal Node**
- Broad, creative, generative  
- Perfect for image transformation, restyling, object edits, isolation  

### **Precise Text Edit Node**
- Controlled, minimal, additive  
- Perfect for polishing assets, expanding images, subtle modifications  


